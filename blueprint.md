
# Lotto Number Generator

## Overview

This is a simple web application that generates random lottery numbers.

## Features

*   Generates 6 unique random numbers between 1 and 45.
*   Displays the generated numbers.
*   Keeps a history of generated numbers.

## Current Task

### Plan

1.  **`index.html`**:
    *   Update the title to "Lotto Number Generator".
    *   Create the basic structure for the lottery number generator.
    *   Add a container for the generated numbers.
    *   Add a button to generate new numbers.
    *   Add a section to display the history of generated numbers.

2.  **`style.css`**:
    *   Add styles for the main container, number display, button, and history.
    *   Ensure the design is clean, modern, and responsive.

3.  **`main.js`**:
    *   Implement the logic to generate 6 unique random numbers.
    *   Add functionality to display the numbers and update the history.
    *   Attach an event listener to the "Generate" button.
